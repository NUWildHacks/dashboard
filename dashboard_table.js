import React from 'react';
import ReachDOM from 'react-dom';
import {Table, Column, ColumnGroup, Cell} from 'fixed-data-table';

class appTable extends React.Component {



  render(){
    return ({
      <Table
        num_apps = {apps.length} // number of table rows
        row_height = {50}
        width = {5000}
        //height = {100}
        //COLUMNS
      >
      </Table>
    });
  }
}
